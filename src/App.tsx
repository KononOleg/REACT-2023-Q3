import { FC } from 'react';
import { HashRouter, Routes } from 'react-router-dom';

const App: FC = () => {
  return (
    <HashRouter>
      <Routes></Routes>
    </HashRouter>
  );
};
export default App;
