export type Pokemon = {
  id: string;
  name: string;
  image: string;
};

export type Result = {
  name: string;
  url: string;
};

export type PokemonDetail = {
  height: string;
  name: string;
  image: string;
  weight: string;
  stats: string[];
};
